<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="view_account_move_customer_form" model="ir.ui.view">
        <field name="name">account.move.customer.form</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">
            <button name="action_invoice_sent" position="before">
                <button
                    type="object"
                    name="button_update_prices_from_pricelist"
                    string="Update prices"
                    attrs="{'invisible': ['|', '|', ('pricelist_id', '=', False), ('state', 'not in', ['draft']), ('type', 'not in', ['out_invoice', 'out_refund', 'in_invoice', 'in_refund'])]}"
                    help="Update price in lines from the pricelist"
                />
            </button>
            <field name="currency_id" position="attributes">
                <attribute name="force_save">1</attribute>
                <attribute
                    name="attrs"
                >{'readonly': [('pricelist_id', '!=', False)]}</attribute>
            </field>
            <field name="journal_id" position="after">
                <field
                    name="pricelist_id"
                    attrs="{'invisible': [('type', 'not in', ['out_invoice', 'out_refund', 'in_invoice', 'in_refund'])]}"
                />
            </field>
            <xpath expr="//field[@name='invoice_line_ids']/tree/field[@name='account_id']" position="after">
                <field name="lot_id" optional="hide" force_save="1" widget="section_and_note_text"/>
                <field name="life_date" optional="hide" force_save="1" widget="section_and_note_text"/>
            </xpath>
            <xpath expr="//field[@name='line_ids']/tree/field[@name='payment_id']" position="after">
                <field name="lot_id" invisible="1" force_save="1"/>
                <field name="life_date" invisible="1" force_save="1"/>
            </xpath>
        </field>
    </record>
    <record id="account_invoice_filter_inherit_account_pricelist" model="ir.ui.view">
        <field name="name">account.invoice.select.inherit.account_pricelist</field>
        <field name="mode">primary</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_account_invoice_filter" />
        <field name="arch" type="xml">
            <xpath expr="//filter[@name='late']" position='before'>
                <separator />
                <filter
                    string="Pricelist"
                    name="pricelist"
                    context="{'group_by':'pricelist_id'}"
                    invisible="context.get('type', '') not in ['out_invoice', 'out_refund']"
                />
                <separator />
            </xpath>
        </field>
    </record>
</odoo>
